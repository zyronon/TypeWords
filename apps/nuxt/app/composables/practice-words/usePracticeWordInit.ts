import type { TaskWords } from '@typewords/core/types/types.ts'

/**
 * Phase 1 占位：initData / loadDict 将在 Phase 2 迁入。
 */
export type PracticeWordInitOptions = {
  initVal?: TaskWords
  fromCache?: boolean
}
